<resources>

    <!-- Application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.DeviceDefault.Light.NoActionBar"></style>

    <style name="AppTheme_Fullscreen" parent="@android:style/Theme.DeviceDefault.Light.NoActionBar.Fullscreen"></style>
</resources>